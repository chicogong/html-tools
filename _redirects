# Cloudflare Pages / Netlify redirects
# https://developers.cloudflare.com/pages/platform/redirects

# Redirect old tool path to new
/tool/*  /tools/:splat  301

# Note: Do NOT add a catch-all 404 redirect here
# It will break static files like sitemap.xml and robots.txt
# Cloudflare Pages handles 404s automatically
